SECRET_KEY=change-me
DATABASE_URL=
ADMIN_USERNAME=
ADMIN_API_TOKEN=
FLASK_DEBUG=0
# Optional: override JSON catalog directory (default: ./sources)
# SOURCES_DIR=/path/to/sources
# Legacy single-file fallback when SOURCES_DIR has no catalog JSON:
# SOURCES_JSON_PATH=/path/to/sources.json
# Show scanlator hostnames on dashboard cards (default: story-first, off)
# UI_SHOW_SOURCE_HOSTS=1
